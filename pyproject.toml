[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-brain-mcp"
version = "0.1.0"
description = "Read-only MCP server over an Obsidian vault: ranked search, note reading and full-text grep."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.28,<2",
    "pydantic>=2.13,<3",
]

# server.py is a script-style module with no main() function, so there is no
# console-script entry point: run it with `python server.py` (see README).
[tool.setuptools]
py-modules = ["server"]
